# CrewAI agent team
crewai>=0.28.0
crewai-tools>=0.4.0

# GitHub API
PyGithub>=2.0.0

# env
python-dotenv>=1.0.0

# CrewAI deps
pydantic>=2.0.0

# Token usage estimation (for cost dashboard; if missing, will approximate by character count)
tiktoken>=0.5.0

# HTTP / external API
requests>=2.28.0

# Dashboard (localhost:3000)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Phase 2 backend stack
langgraph>=0.2.0
redis>=5.0.0
psycopg[binary]>=3.2.0
